{
  "buildCommand": "cd frontend && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/football-api/:path*",
      "destination": "/api/football-api/:path*"
    }
  ]
}
